import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    root: {
        display: 'flex',
        flexFlow: 'row nowrap'
    },
    badge: {
        width: '20px',
        backgroundColor: 'transparent'
    },
    tree: {
        width: '150px'
    }
});